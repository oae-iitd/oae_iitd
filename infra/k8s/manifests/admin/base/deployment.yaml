apiVersion: apps/v1
kind: Deployment
metadata:
  name: oae-admin
  namespace: oae
  labels:
    app: oae-admin
spec:
  replicas: 2
  selector:
    matchLabels:
      app: oae-admin
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: oae-admin
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: admin
          image: oae/admin-client:latest   # replaced by CI with ECR URL + SHA tag
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 20
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: oae-admin
                topologyKey: kubernetes.io/hostname
