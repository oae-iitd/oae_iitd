apiVersion: v1
kind: ServiceAccount
metadata:
  name: oae-server
  namespace: oae
  annotations:
    # Populated by overlay with the IRSA role ARN from Terraform output
    eks.amazonaws.com/role-arn: REPLACE_WITH_IRSA_ROLE_ARN
